---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
import { getCollection } from 'astro:content';

const posts = (await getCollection('blog')).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);
---

<!doctype html>
<html lang="ja">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
	</head>
	<body>
		<Header />
		<main>
			<h1>🔍 エラー解決データベース</h1>
			<p>ComfyUIのエラーと解決策をAIが24時間体制で収集・翻訳しています。</p>
			
			<div style="margin-top: 2rem;">
				{posts.map((post) => (
					<div style="border: 1px solid #333; padding: 1rem; margin-bottom: 1rem; border-radius: 8px;">
						<a href={`/blog/${post.slug}/`} style="text-decoration: none; color: inherit;">
							<h3 style="margin: 0;">{post.data.title}</h3>
							<p style="margin: 0.5rem 0 0; font-size: 0.9rem; color: #888;">
								{post.data.pubDate.toLocaleDateString('ja-JP')}
							</p>
						</a>
					</div>
				))}
			</div>
		</main>
		<Footer />
	</body>
</html>
